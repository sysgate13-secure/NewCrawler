name: Daily Security News Crawler

on:
  schedule:
    - cron: '0 23 * * *'  # 매일 한국시간 오전 8시 (UTC 23시)
  workflow_dispatch:  # 수동 실행 가능

jobs:
  crawl:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
        
    - name: Install dependencies
      run: |
        pip install -r requirements.txt
        
    - name: Run crawler
      run: |
        python crawler.py
        
    - name: Commit and push if changed
      run: |
        git config --global user.email "250818752+sysgate13-secure@users.noreply.github.com"
        git config --global user.name "sysgate13-secure"
        git add security_news.db
        git diff --quiet && git diff --staged --quiet || (git commit -m "🤖 Auto-crawl: $(date +'%Y-%m-%d %H:%M')" && git push)
